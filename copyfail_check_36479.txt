=== CVE-2026-31431 (Copy Fail) -- AF_ALG Mitigation Check ===

This checks ONE of Microsoft's two official mitigation paths
(block AF_ALG socket creation) -- independent of and
complementary to Watchdog's existing kernel-version check for
this same CVE, which is separately disclosed as fragile to
backports.

[NOT MITIGATED VIA THIS PATH] AF_ALG socket creation SUCCEEDED -- this specific mitigation path is NOT in place. Does not mean the kernel is unpatched, only that this particular attack surface is reachable.

This does not confirm the host is vulnerable -- only that
this specific mitigation is absent. Cross-reference against
the kernel-version check and your distribution's own
advisory before drawing a conclusion either way.
