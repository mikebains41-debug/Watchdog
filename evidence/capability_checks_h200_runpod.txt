CAPABILITY CHECKS, H200 x4 RunPod, driver 570.124.06

PCIe telemetry        static link info YES; real-time dmon throughput MAYBE
                      (script correctly refuses to conclude without a human
                      reading raw output during an ACTIVE transfer -- not idle)
ECC fields            READABLE on all 4 GPUs. Baseline 0 corrected /
                      0 uncorrected, pstate P0, 31-36C.
                      Tier-2 Rowhammer telemetry path CONFIRMED.
/proc/net/tcp         READABLE inside the container. egress_collector.py has a
                      working data source on RunPod.
compute_apps          EMPTY even with a live workload (preflight + remediation
                      both confirmed). Container cannot see process PIDs; the
                      driver reports them in the host PID namespace.
                      CONSEQUENCE: VRAMResidualDetector cannot evaluate here,
                      and no detector can supply a PID to kill_process.

/dev/nvidia*          devices 0,3,4,6 present (NON-CONTIGUOUS). Container maps
                      them to CUDA indices 0-3. Host device numbering differs
                      from container numbering -- relevant to any host-side
                      correlation.
/dev/shm              469G available, 0% used, CLEAN. No prior-tenant files.
                      Consistent with the 2/2 clean RunPod scans in
                      B200_FINDINGS_REPORT (vs 5/5 dirty on Vast.ai/H200).

ROUND 2 -- the four covert-channel capability questions, ANSWERED:
TLB miss counters      NOT PRESENT. nvidia-smi -q -d PERFORMANCE,UTILIZATION
                       returns no tlb/cache/compression fields at all.
cache-eviction perf    NOT PRESENT (same query, same result).
GPU.zip compression    NOT PRESENT / not reachable from NVML.
Baddour micro-arch     NOT PRESENT -- no underlying counter exists here.
  => DO NOT BUILD these four detectors for H200. The telemetry does not exist.

Fields that ARE readable and are detector-viable:
  utilization.memory              OK (0%)
  encoder.stats.averageFps        OK (0)
  clocks_throttle_reasons.active  OK (0x...01 = GpuIdle, correct at idle)
  pcie.link.gen.current           OK (Gen 5 on all four)
  retired_pages.pending           [N/A] on H200 -- uses row-remapping, not
                                  page retirement. A retired-pages detector
                                  would be silent on this architecture.

COVERT-COMPUTE INDUCER, corrected:
  harness inducer  8192^2 bf16 GEMM,  5s  -> 57% util, 524.54 W  (BELOW the
                   detector's >90% util threshold -- stage FAILED for this
                   reason, not because the detector is wrong)
  proper inducer   16384^2 bf16 GEMM, 60s -> 100% util, 684.08 W, SM 1455 MHz
                   (684 W against a 700 W limit; SM settles from 1980 boost to
                   1455 sustained -- the flat near-TDP signature
                   CovertMiningDetector is built for)
  => FIX NEEDED in validation_harness: the covert_compute inducer needs a
     larger matrix and a longer run to reach the threshold it is testing.
