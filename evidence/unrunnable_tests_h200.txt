TWO TEST FILES HAVE NEVER BEEN EXECUTABLE
Found on real hardware, 4x H200 RunPod, 2026-09-20

tests/false_positive_benchmark.py
tests/latency_test.py
tests/stability_test.py

Both call:
    TelemetryCollector(hz=self.hz)     -> TypeError, no such kwarg
    collector.collect()                -> AttributeError, no such method

The real API is:
    agent/telemetry.py:300
      def __init__(self, sample_hz=SAMPLE_HZ, output_dir='watchdog_data',
                   gpu_index=None, ...)
    public methods on TelemetryCollector: ['start', 'stop']   -- that is all

Renaming hz -> sample_hz gets past the constructor and then fails on
collector.collect(). These files were written against an API that has never
existed in this repo. They cannot be fixed by a parameter rename; they need
rewriting around start/stop, or around sample_gpu() directly.

CONSEQUENCE: the false-positive RATE benchmark and the detection LATENCY
test have never produced a number. Both are figures that would otherwise be
quotable. Neither is covered by run_all.py's TEST_FILES list, so nothing
flagged that they were broken.

(Local edits reverted -- the repo is left as found, with the defect
documented rather than half-fixed on a pod.)

THIRD FILE, same defect: tests/stability_test.py also calls
  TelemetryCollector(hz=self.hz) -> TypeError. Never executable either.

ALSO FOUND, these DID run on hardware:
  resource_benchmark.py  10,000 rows in 6.18s = 1,618 rows/sec.
                         Memory min 12.0 MB, max 12.0 MB, variance 0.0 MB
                         across 10k rows -- no leak in the pipeline.
                         CPU ~100% single-core.
  competitor_benchmark.py  3 confirmed structural advantages, 1 pending
                         real-hardware validation. Its own note: "does not
                         include a live side-by-side benchmark against
                         installed competitor tools -- every claim above is
                         traceable to a specific existing finding."
  burnin_stability.py    TERMINATED at a 180s timeout. Long-running by
                         design; needs a longer window or a duration flag.

CORRECTION -- run_all_tests.py already excludes these four, with this note:
  "stability_test.py, false_positive_benchmark.py, latency_test.py, and
   swarm_test.py require real GPU hardware / running servers and are NOT
   included in this automated suite -- run them manually when hardware is
   available."

The EXCLUSION is deliberate. The stated REASON is wrong for three of them.
We are on real GPU hardware (4x H200) and they still fail -- not because
hardware is missing, but because they call an API that does not exist
(TelemetryCollector(hz=) and .collect()). Only swarm_test.py genuinely
needs something absent: running Watchdog API servers (0/1 reachable).
FIX: correct the note, or fix the three files. As written it implies a
hardware gap where there is a code defect.

OTHER RESULTS THIS ROUND
  resource_benchmark.py reproduced: 1,632 rows/sec (first run 1,618),
    memory variance 0.0 MB both times.
  repo_hygiene_audit.py: BARE_EXCEPT sweep, "701 more..." beyond the shown
    lines, concentrated in todo/module19.py and todo/module20.py -- the
    dormant module directory, consistent with the earlier 486+258 count.
  audit_dead_imports.py: correctly classified intelligence/threat_intel.py
    as LIVE -- imported AND called by threat_intel_airgap.py,
    RECOMMENDATION KEEP. The AST-based fix works; the earlier regex version
    would have deleted a live base class.
  Container Escape: FAIL (/proc visible). Compliance Evidence: PASS.

MY OWN TOOLING BUGS, exact lines:
  scripts/remediation_live_test.py:273-274
    imports ClusterActions; the real class is ClusterOrchestration.
    This is why its check 9 failed with ImportError.
  scripts/sigkill_residual_measure.py:197
    hardcodes verdict "SIGKILL_CLEARS_ACCOUNTING" regardless of --signal,
    so the SIGTERM run is labelled SIGKILL. Measurement correct, label wrong.
  tests/test_pod_runner.py
    exceeds tests/run_all.py:81 timeout=120 on a real-GPU host because it
    shells out to pod_runner.py --live --allow-fake several times. Instant
    on a phone, slow on a GPU box. Blocks run_all.py from completing.
